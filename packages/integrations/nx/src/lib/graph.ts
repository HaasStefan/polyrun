import type { TaskGraphFactory, TaskGraph } from '@polyrun/contracts';

export const createTaskGraphAsync: TaskGraphFactory = async (
  command: string
): Promise<TaskGraph> => {
  // Implementation for creating a task graph for NX
  // todo

  return {};
};
